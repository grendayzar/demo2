# Supabase (project: Promo Routes)
NEXT_PUBLIC_SUPABASE_URL=https://tapsapvdgolcyaweqarr.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_-k3Z9rPibP3N46cQ1EISSQ_dKNSyKQi
# Server only. Supabase dashboard -> Project Settings -> API keys -> service_role (or a secret key).
SUPABASE_SERVICE_ROLE_KEY=

# Public URL of the deployed app (used for QR codes and email links)
NEXT_PUBLIC_APP_URL=https://promo-routes.vercel.app

# Email (Resend). Leave empty to log emails to the console instead of sending.
RESEND_API_KEY=
EMAIL_FROM="Accident Professionals <no-reply@accidentprofessionals.com>"
# Where lead emails and contract requests land when no per-store inbox is set
LEADS_INBOX_EMAIL=
CONTRACTS_INBOX_EMAIL=

# Optional shared secret to sign outbound lead webhooks (X-Signature header, HMAC-SHA256 of the body)
LEAD_WEBHOOK_SECRET=
